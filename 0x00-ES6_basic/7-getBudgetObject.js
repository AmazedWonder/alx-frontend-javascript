export default function getBudgetObject(income, gdp, capita) {
  const budget = {
    income, // Shorthand property name
    gdp, // Shorthand property name
    capita, // Shorthand property name
  };

  return budget;
}
